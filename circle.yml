test:
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/joker-engine/cobertura
    - mkdir -p $CIRCLE_ARTIFACTS/joker-engine/surefire-reports
    - find . -type f -regex "joker-engine/target/surefire-reports/*" -exec cp {} $CIRCLE_ARTIFACTS/joker-engine/surefire-reports/ \;
    - find . -type f -regex "joker-engine/target/site/cobertura/*" -exec cp {} $CIRCLE_ARTIFACTS/joker-engine/cobertura \;
